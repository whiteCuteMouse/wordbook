import React from "react"
import ReactDOM from "react-dom"
import _custom_button from "./custom_button"

ReactDOM.render(
  <React.StrictMode>
        <_custom_button />
  </React.StrictMode>,
  document.getElementById("root")
)
